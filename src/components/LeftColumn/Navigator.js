import React from 'react';


/**
 * This represents a component to help navigate the site.
 * The idea is to place it in the left column
 */
const Navigator = (props) => (
    <div>
    </div>
);


export default Navigator;